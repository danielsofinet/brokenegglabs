# Session 2026-06-17 — brokenegglabs.com fixes (post-revert)

## Important history
An earlier attempt this day REPLACED the WordPress bundle (dropped main.js, custom
router) — it broke the marquees/intro/animations and was REVERTED. Decision: KEEP the
original bundle, layer all fixes in bke.js/bke.css. Don't repeat the rip-out.

## Fixes shipped (all on top of the original bundle)
1. Contact modal → Web3Forms (daniel.sofinet@gmail.com); removed hello@brokenegg.labs.
2. Dates → 2026; phone → 06 73 67 22 00.
3. Videos localized + compressed (assets/video/), progressive autoplay on view.
4. Clean URL: address bar stays / on every page (history override).
5. Project pages: asset/link paths made root-absolute so images load under the / URL;
   added class="white" to project nav links (light nav on dark pages).
6. Process slider (#sectionPin): dead GSAP pin-scroll replaced with drag/wheel/swipe.
7. Replacement nav (.bke-nav): hide the GSAP-pinned header, render our own fixed clone
   with a smooth auto-hide fold + light/dark recolour, consistent on every page.
8. Cleanup: removed unused wp-embed.min.js, empty theme style.css, unused WP block
   library style.min.css.

## Key diagnoses (so future sessions don't re-discover the hard way)
- Barba.js IS working (page transitions) even though window.barba is undefined (module-scoped).
- The nav fold/jank is GSAP with inline !important transforms in main.js — can't be
  overridden; the fix is the replacement nav, not patching the original header.
- The Process slider's GSAP pin-scroll never initialises (track transform stays none),
  so it's free to take over with our own scroller.
- URL-stays-/ forces document base to /, which breaks folder-relative asset paths —
  hence the root-absolute path fix on project pages.

## Verified in-browser (Chrome DevTools MCP)
Home/About/Work/Contact/project: render, nav colours, nav fold, slider drag, project
media, contact modal, clean URL across multi-page navigation. Console clean (only a
benign Barba info log).

## Possible follow-ups
- Hard-refresh returns to homepage (side effect of URL-stays-/). Acceptable per Daniel.
- main.css still large/untrimmed (kept; risky to trim).
- Send one real test message from the live contact form to confirm delivery.
